# ✅ Чеклист готовности для тестирования алгоритмов

## 🔍 Статус проверки

### ✅ Сервисы запущены
- [x] API Gateway работает на http://localhost:8000
- [x] Celery Worker запущен (обрабатывает задачи)
- [x] PostgreSQL подключен и работает
- [x] Redis подключен и работает
- [x] RabbitMQ подключен и работает

### ✅ Данные в системе
- [x] 30 торговых пар загружены в базу
- [x] 100 сигналов уже в базе данных
- [x] Уровни сохраняются в базу данных

### ⚠️ Требует внимания

#### 1. Сохранение сигналов в БД
**Текущая ситуация:** 
- `signal_manager.save_signal()` сохраняет сигналы в JSON файлы
- Сигналы из анализа не автоматически сохраняются в PostgreSQL

**Решение:**
- Нужно обновить `core/analysis_engine.py` чтобы вызывать Celery задачу `process_new_signal` при генерации сигнала
- Или обновить `signal_manager.save_signal()` чтобы сохранять в БД

#### 2. Отрисовка уровней на графиках
**Текущая ситуация:**
- ✅ API endpoint `/api/chart-data` возвращает уровни
- ✅ Frontend код для отрисовки уровней есть в `dashboard.html`
- ✅ Уровни отображаются как горизонтальные линии на графике

**Проверка:**
- Откройте Dashboard: http://localhost:8000/dashboard
- Кликните на любую торговую пару
- Проверьте, что уровни отображаются на графике

#### 3. Запись сделок в сигналы
**Текущая ситуация:**
- Сигналы генерируются в `analysis_engine.py` при анализе пар
- Сигналы сохраняются через `signal_manager.save_signal()` в JSON
- НО: сигналы не автоматически сохраняются в PostgreSQL БД

## 🔧 Что нужно исправить перед тестированием

### 1. Интеграция сохранения сигналов в БД

**Вариант A (рекомендуется):** Обновить `signal_manager.save_signal()` чтобы сохранять в БД:
```python
def save_signal(self, signal_data):
    # Сохранить в JSON (для совместимости)
    # Сохранить в PostgreSQL через Celery задачу
    process_new_signal.delay(signal_data)
```

**Вариант B:** Обновить `analysis_engine.py` чтобы вызывать задачу напрямую:
```python
from tasks.signals_tasks import process_new_signal
# ...
process_new_signal.delay(signal_data)
```

### 2. Проверка работы анализа

```bash
# Запустить анализ всех пар
curl -X POST http://localhost:8000/api/force-analysis

# Проверить статус задач
curl http://localhost:8000/api/tasks

# Проверить новые сигналы
curl http://localhost:8000/api/signals | python3 -m json.tool
```

## 📋 План тестирования

### 1. Тест генерации сигналов
- [ ] Запустить анализ пар через API
- [ ] Проверить, что сигналы создаются в БД
- [ ] Проверить страницу сигналов: http://localhost:8000/signals

### 2. Тест отрисовки уровней
- [ ] Открыть Dashboard: http://localhost:8000/dashboard
- [ ] Кликнуть на торговую пару с уровнями
- [ ] Проверить, что уровни отображаются на графике
- [ ] Проверить, что уровни отображаются как горизонтальные линии

### 3. Тест записи сигналов
- [ ] Проверить, что новые сигналы появляются в таблице
- [ ] Проверить, что сигналы имеют правильные поля (entry_price, level_price, etc.)
- [ ] Проверить, что P&L обновляется через задачу `update-signals-pnl`

## 🚀 Быстрый старт тестирования

1. **Запустить анализ:**
   ```bash
   curl -X POST http://localhost:8000/api/force-analysis
   ```

2. **Проверить результаты:**
   - Dashboard: http://localhost:8000/dashboard
   - Signals: http://localhost:8000/signals
   - API Tasks: http://localhost:8000/api/tasks

3. **Проверить логи:**
   ```bash
   tail -f /tmp/celery_worker.log
   tail -f /tmp/api_gateway.log
   ```

## ⚠️ Известные проблемы

1. **Сигналы сохраняются в JSON, но не автоматически в БД**
   - Нужно обновить интеграцию (см. выше)

2. **Уровни могут не отображаться, если нет активных уровней**
   - Проверьте через API: `curl http://localhost:8000/api/levels`

