# OwnedCore - CryptoProject v0.01

🚀 **Профессиональная система анализа криптовалютных рынков с микросервисной архитектурой**

## 📋 Описание проекта

OwnedCore - это современная система анализа криптовалютных рынков, реализующая стратегию **Royal Levels** на основе отскоков от уровней поддержки и сопротивления. Система анализирует 30 популярных торговых пар в реальном времени, генерирует торговые сигналы и предоставляет интерактивные графики для визуального анализа.

### 🏗️ Архитектура

- **API Gateway** - FastAPI для обработки запросов
- **PostgreSQL** - база данных для хранения данных
- **Redis** - кэширование и быстрый доступ
- **Celery + RabbitMQ** - фоновые задачи и очереди
- **Docker** - контейнеризация сервисов

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env при необходимости
```

### 3. Запуск Docker сервисов

```bash
cd docker
docker-compose up -d
```

### 4. Инициализация базы данных

```bash
# Создание миграций
alembic revision --autogenerate -m "Initial migration"

# Применение миграций
alembic upgrade head

# Миграция данных из JSON (если есть)
python scripts/migrate_json_to_db.py
```

### 5. Запуск системы

**Терминал 1: API Gateway**
```bash
./START_API.sh
```

**Терминал 2: Celery Worker**
```bash
./START_CELERY.sh
```

### 6. Проверка работы

```bash
# Health check
curl http://localhost:8000/health

# Откройте в браузере
open http://localhost:8000/dashboard
```

## 📁 Структура проекта

```
OwnedCore-CryptoProject-v0.01/
├── core/                    # Основные модули
│   ├── analysis_engine.py  # Движок анализа стратегии
│   ├── signal_manager.py   # Управление сигналами
│   ├── database.py         # Подключение к БД
│   ├── cache.py            # Redis кэш
│   ├── config.py           # Настройки
│   └── models.py           # SQLAlchemy модели
├── services/
│   └── api_gateway/        # API Gateway (FastAPI)
│       └── main.py
├── tasks/                   # Celery задачи
│   ├── celery_app.py
│   ├── analysis_tasks.py
│   └── signals_tasks.py
├── web/                     # Веб-интерфейс
│   ├── dashboard.html
│   └── signals.html
├── docker/                  # Docker конфигурация
│   └── docker-compose.yml
├── scripts/                 # Вспомогательные скрипты
├── migrations/              # Alembic миграции
├── START_API.sh            # Скрипт запуска API
├── START_CELERY.sh         # Скрипт запуска Celery
└── requirements.txt         # Зависимости
```

## 🔌 API Endpoints

### Основные эндпоинты:

- `GET /` - Информация о сервисе
- `GET /health` - Проверка здоровья системы
- `GET /api/pairs-status` - Статус всех торговых пар
- `GET /api/signals` - История сигналов
- `GET /api/levels` - Активные уровни
- `GET /dashboard` - Веб-интерфейс дашборда
- `GET /signals` - Веб-интерфейс сигналов
- `GET /docs` - Swagger документация API

## 🎯 Стратегия Royal Levels

### Основные принципы:

1. **Строгий трендовый фильтр (1H)**
   - Восходящий тренд → только лонг-сделки
   - Нисходящий тренд → только шорт-сделки
   - Боковой тренд → ожидание

2. **Фрактальный анализ (15m)**
   - Поиск локальных минимумов/максимумов
   - Динамический lookback (5-100 свечей)
   - Фильтр качества: высокие объемы + длинные свечи

3. **Система уровней**
   - Уровни создаются на основе фракталов
   - Подсчет исторических касаний (test_count)
   - Валидация уровней (максимум 2 теста)

4. **Генерация сигналов**
   - Второе касание уровня в реальном времени
   - Соответствие направлению тренда
   - Запись сигнала для анализа

## 🛠️ Управление системой

### Запуск компонентов

```bash
# API Gateway
./START_API.sh

# Celery Worker
./START_CELERY.sh
```

### Остановка компонентов

```bash
# Остановить API Gateway
pkill -f "services/api_gateway/main.py"

# Остановить Celery Worker
pkill -f "celery.*worker"

# Остановить Docker сервисы
cd docker
docker-compose down
```

### Мониторинг

```bash
# Проверка статуса
curl http://localhost:8000/health

# Логи API Gateway
tail -f /tmp/api_gateway.log

# Логи Celery
tail -f /tmp/celery_worker.log

# Docker сервисы
docker ps
docker logs ownedcore_postgres
docker logs ownedcore_redis
docker logs ownedcore_rabbitmq
```

## 📚 Дополнительная документация

- `QUICK_START.md` - Быстрый старт за 2 команды
- `docs/SETUP_OPTIONS.md` - Варианты настройки (Docker vs локально)
- `docs/DRAWING_TOOLS_GUIDE.md` - Руководство по инструментам рисования на графиках
- `docs/UI_IMPROVEMENTS.md` - Описание улучшений интерфейса

## 🔧 Настройка

### Переменные окружения (.env)

```env
# База данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ownedcore
DB_USER=postgres
DB_PASSWORD=postgres

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# RabbitMQ
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# API
API_HOST=0.0.0.0
API_PORT=8000
```

## 🚨 Устранение неполадок

### Проблемы с подключением к БД

```bash
# Проверка PostgreSQL
docker exec ownedcore_postgres psql -U postgres -d ownedcore -c "SELECT 1;"
```

### Проблемы с Redis

```bash
# Проверка Redis
docker exec ownedcore_redis redis-cli ping
```

### Проблемы с RabbitMQ

```bash
# Проверка RabbitMQ
docker exec ownedcore_rabbitmq rabbitmq-diagnostics ping
```

## 📊 Технические характеристики

- **Python 3.8+**
- **FastAPI** - современный ASGI фреймворк
- **PostgreSQL** - реляционная БД
- **Redis** - кэширование
- **Celery** - фоновые задачи
- **SQLAlchemy** - ORM
- **Alembic** - миграции БД
- **Docker** - контейнеризация

## 📄 Лицензия

MIT License

---

**OwnedCore - CryptoProject v0.01** - Профессиональный анализ криптовалютных рынков 🚀
