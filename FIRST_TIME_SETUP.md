# 🚀 Первоначальная настройка системы

## Если вы видите пустые страницы без данных

Выполните следующие шаги для заполнения базы данных:

### Шаг 1: Инициализация торговых пар

```bash
python3 scripts/init_data.py
```

Это добавит 30 торговых пар в базу данных.

### Шаг 2: Запуск анализа

```bash
curl -X POST http://localhost:8000/api/force-analysis
```

**Или используйте автоматический скрипт:**

```bash
./scripts/load_data.sh
```

### Шаг 3: Ожидание результатов

Анализ всех 30 пар займет **1-2 минуты**. 

Проверьте статус:
```bash
curl http://localhost:8000/api/pairs-status
```

Когда увидите `"status": "success"` и данные по парам - все готово!

### Шаг 4: Обновите страницу в браузере

Откройте:
- http://localhost:8000/dashboard
- http://localhost:8000/signals

Данные должны появиться автоматически.

## ⚠️ Важно

1. **Celery Worker должен быть запущен** - без него анализ не выполнится
2. **API Gateway должен быть запущен** - для обработки запросов
3. **Подождите 1-2 минуты** - анализ занимает время

## 🔄 Что происходит при анализе

1. Система получает данные с Binance API для всех 30 пар
2. Анализирует тренды, уровни поддержки/сопротивления
3. Генерирует сигналы при касании уровней
4. Сохраняет все данные в PostgreSQL
5. Кэширует результаты в Redis

## 📝 После первого запуска

Система будет автоматически:
- Обновлять данные каждые 10 минут
- Показывать актуальную информацию
- Генерировать новые сигналы

## 🐛 Проблемы?

См. `docs/DATA_LOADING.md` для подробной диагностики.

