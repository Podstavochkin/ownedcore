# OwnedCore - Trading System

Профессиональная система автоматической торговли криптовалютами с детекцией уровней поддержки/сопротивления, анализом паттернов и генерацией торговых сигналов.

## 🚀 Основные возможности

- **Детекция уровней поддержки/сопротивления** с автоматическим обновлением
- **Генерация торговых сигналов** на основе уровней и паттернов
- **Детекция паттернов** (треугольники, свечные паттерны)
- **Фильтрация сигналов** по Elder's Screens и другим критериям
- **Демо-торговля** с управлением рисками
- **Веб-интерфейс** с интерактивными графиками (Lightweight Charts)
- **Фоновые задачи** через Celery для непрерывного мониторинга

## 📋 Структура проекта

```
├── core/                    # Основная логика системы
│   ├── analysis_engine.py  # Детекция уровней и анализ
│   ├── chart_patterns_detector.py  # Детекция паттернов
│   ├── trading/            # Торговые модули
│   └── ...
├── tasks/                   # Celery задачи
│   ├── level_touch_tasks.py  # Мониторинг касаний уровней
│   ├── demo_trading_tasks.py  # Демо-торговля
│   └── ...
├── web/                     # Веб-интерфейс
│   ├── charts.html         # Интерактивные графики
│   ├── dashboard.html      # Дашборд
│   └── ...
├── scripts/                 # Вспомогательные скрипты
└── docs/                    # Документация
```

## 🛠️ Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/Podstavochkin/ownedcore.git
   cd ownedcore
   ```

2. **Установите зависимости:**
   ```bash
   pip install -r config/requirements.txt
   ```

3. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env файл с вашими настройками
   ```

4. **Инициализируйте базу данных:**
   ```bash
   alembic upgrade head
   ```

5. **Запустите систему:**
   ```bash
   ./START_ALL.sh
   ```

## 📖 Документация

Подробная документация находится в папке `docs/`:
- `FIRST_TIME_SETUP.md` - Первоначальная настройка
- `QUICK_START.md` - Быстрый старт
- `PROJECT_WORKFLOW.md` - Рабочий процесс
- И другие...

## 🔒 Безопасность

⚠️ **ВАЖНО:** Файл `.env` содержит секретные данные и НЕ должен попадать в репозиторий. Используйте `.env.example` как шаблон.

## 📝 Лицензия

Проект находится в разработке.

## 🤝 Вклад

Проект в активной разработке. Все изменения отслеживаются через GitHub Issues и Pull Requests.

