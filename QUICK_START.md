# ⚡ Быстрый старт

## 🚀 Запуск за 1 команду (рекомендуется)

```bash
./START_ALL.sh
```

Этот скрипт автоматически:
- ✅ Запускает Docker контейнеры (PostgreSQL, Redis, RabbitMQ)
- ✅ Запускает API Gateway
- ✅ Запускает Celery Worker
- ✅ Запускает Celery Beat (периодические задачи)
- ✅ Проверяет статус всех сервисов

**⚠️ Важно:** После запуска подождите 1-2 минуты, пока Celery Worker обработает все 30 пар. Данные появятся автоматически на страницах.

---

## 🚀 Ручной запуск (если нужно запускать компоненты отдельно)

### Предварительные требования

1. **Docker сервисы должны быть запущены:**
```bash
cd docker
docker-compose up -d
```

2. **База данных должна быть инициализирована:**
```bash
# Применить миграции
alembic upgrade head

# Инициализация торговых пар
python3 scripts/init_data.py
```

### Запуск компонентов

**Терминал 1: API Gateway**
```bash
./START_API.sh
```

**Терминал 2: Celery Worker**
```bash
./START_CELERY.sh
```

**Терминал 3: Celery Beat (периодические задачи)**
```bash
./START_BEAT.sh
```

### Загрузка данных в систему

После запуска всех компонентов:

```bash
# Запустить анализ через API (это займет 1-2 минуты)
curl -X POST http://localhost:8000/api/force-analysis

# Проверить статус
curl http://localhost:8000/api/pairs-status
```

**⚠️ Важно:** После запуска анализа подождите 1-2 минуты, пока Celery Worker обработает все 30 пар. Данные появятся автоматически на страницах.

---

## 🛑 Остановка проекта

```bash
./STOP_ALL.sh
```

Этот скрипт автоматически останавливает все компоненты:
- ✅ API Gateway
- ✅ Celery Worker
- ✅ Celery Beat
- ✅ Docker контейнеры

---

## ✅ Проверка

```bash
# Health check
curl http://localhost:8000/health

# Проверка статуса задач
curl http://localhost:8000/api/tasks

# Откройте в браузере
open http://localhost:8000/dashboard
```

---

## 🔗 Полезные ссылки

- **Dashboard**: http://localhost:8000/dashboard
- **Signals**: http://localhost:8000/signals
- **API Docs**: http://localhost:8000/docs
- **Health Check**: http://localhost:8000/health
- **RabbitMQ UI**: http://localhost:15672 (guest/guest)

---

## 📋 Периодические задачи

Система автоматически выполняет следующие задачи:

- **Каждые 5 минут**: Анализ всех торговых пар, поиск уровней, проверка пробитых уровней
- **Каждую минуту**: Обновление P&L сигналов, проверка касаний уровней (скальпинг)

---

## 📝 Логи

```bash
# Логи API Gateway
tail -f /tmp/api_gateway.log

# Логи Celery Worker
tail -f /tmp/celery_worker.log

# Логи Celery Beat
tail -f /tmp/celery_beat.log
```

---

## 📖 Подробная документация

См. [PROJECT_WORKFLOW.md](./PROJECT_WORKFLOW.md) для полного руководства по работе с проектом.

---

**Готово!** 🎉
