# Инструкция по загрузке проекта в GitHub

## ✅ Что уже сделано:

1. ✅ Git репозиторий инициализирован
2. ✅ Remote origin настроен: `https://github.com/Podstavochkin/ownedcore.git`
3. ✅ Ветка: `main`
4. ✅ `.gitignore` обновлен (добавлены `.env`, `celerybeat-schedule*`)
5. ✅ Все файлы добавлены в staging
6. ✅ Первый коммит создан

## ⚠️ ВАЖНО: Проверка безопасности

**Перед пушем убедитесь, что `.env` файл НЕ попал в коммит:**

```bash
# Проверка, что .env не в коммите
git log -1 --name-only | grep "\.env"
```

Если `.env` есть в выводе - **НЕ ПУШИТЬ!** Нужно будет исправить коммит.

## 🚀 Шаги для загрузки в GitHub

### Вариант 1: Использование Personal Access Token (рекомендуется)

1. **Создайте Personal Access Token на GitHub:**
   - Перейдите: https://github.com/settings/tokens
   - Нажмите "Generate new token (classic)"
   - Выберите scope: `repo` (полный доступ к репозиториям)
   - Скопируйте токен

2. **Используйте токен для пуша:**
   ```bash
   git push -u origin main
   ```
   Когда попросит username: введите ваш GitHub username
   Когда попросит password: вставьте Personal Access Token (не пароль!)

### Вариант 2: Использование SSH (более безопасно)

1. **Настройте SSH ключ (если еще не настроен):**
   ```bash
   # Проверка существующих ключей
   ls -la ~/.ssh
   
   # Если нет ключа, создайте:
   ssh-keygen -t ed25519 -C "your_email@example.com"
   
   # Добавьте ключ в ssh-agent
   eval "$(ssh-agent -s)"
   ssh-add ~/.ssh/id_ed25519
   
   # Скопируйте публичный ключ
   cat ~/.ssh/id_ed25519.pub
   ```

2. **Добавьте SSH ключ на GitHub:**
   - Перейдите: https://github.com/settings/keys
   - Нажмите "New SSH key"
   - Вставьте содержимое `~/.ssh/id_ed25519.pub`

3. **Измените remote на SSH:**
   ```bash
   git remote set-url origin git@github.com:Podstavochkin/ownedcore.git
   ```

4. **Запушьте:**
   ```bash
   git push -u origin main
   ```

### Вариант 3: GitHub CLI (самый простой)

```bash
# Установите GitHub CLI (если еще не установлен)
brew install gh

# Авторизуйтесь
gh auth login

# Запушьте
git push -u origin main
```

## 📋 Команды для проверки перед пушем

```bash
# Проверить, что .env не в коммите
git log -1 --name-only | grep "\.env" || echo "✅ .env не в коммите"

# Проверить статус
git status

# Проверить remote
git remote -v

# Посмотреть последний коммит
git log -1 --stat
```

## 🔒 Безопасность

**КРИТИЧЕСКИ ВАЖНО:**
- ✅ `.env` файл должен быть в `.gitignore`
- ✅ `.env` НЕ должен быть в коммите
- ✅ Если `.env` уже попал в историю, нужно удалить его:
  ```bash
  git filter-branch --force --index-filter \
    "git rm --cached --ignore-unmatch .env" \
    --prune-empty --tag-name-filter cat -- --all
  ```

## 📝 После успешного пуша

1. Проверьте репозиторий: https://github.com/Podstavochkin/ownedcore
2. Убедитесь, что все файлы загружены
3. Проверьте, что `.env` НЕ виден в репозитории
4. Создайте файл `.env.example` с примером переменных (без реальных значений)

## 🔄 Дальнейшая работа

После настройки, для каждого изменения:
```bash
git add .
git commit -m "Описание изменений"
git push
```

