# 📊 Загрузка данных в систему

## Проблема: Пустые данные на страницах

Если вы видите пустые страницы без данных о торговых парах и сигналах, выполните следующие шаги:

## ✅ Решение

### Шаг 1: Инициализация торговых пар

Добавьте торговые пары в базу данных:

```bash
python3 scripts/init_data.py
```

Этот скрипт добавит 30 торговых пар из `TRADING_PAIRS` в базу данных.

### Шаг 2: Запуск анализа

Запустите анализ всех пар через API:

```bash
curl -X POST http://localhost:8000/api/force-analysis
```

Или используйте автоматический скрипт:

```bash
./scripts/load_data.sh
```

### Шаг 3: Ожидание результатов

Анализ может занять 1-2 минуты. Проверьте статус:

```bash
curl http://localhost:8000/api/pairs-status
```

Когда анализ завершится, вы увидите:
- `"status": "success"` вместо `"processing"`
- Данные по всем парам с ценами, трендами, объемами

### Шаг 4: Проверка данных

```bash
# Проверить сигналы
curl http://localhost:8000/api/signals

# Проверить уровни
curl http://localhost:8000/api/levels
```

## 🔄 Автоматическое обновление

После первого запуска анализа, система будет автоматически:
- Обновлять данные каждые 10 минут (через Celery задачи)
- Кэшировать результаты в Redis
- Показывать актуальные данные на страницах

## ⚠️ Важные моменты

1. **Celery Worker должен быть запущен** - без него анализ не выполнится
2. **API Gateway должен быть запущен** - для обработки запросов
3. **Docker сервисы должны работать** - PostgreSQL, Redis, RabbitMQ

## 🐛 Если данные все еще не появляются

1. Проверьте логи Celery:
```bash
tail -f /tmp/celery_worker.log
```

2. Проверьте логи API Gateway:
```bash
tail -f /tmp/api_gateway.log
```

3. Проверьте подключение к БД:
```bash
docker exec ownedcore_postgres psql -U postgres -d ownedcore -c "SELECT COUNT(*) FROM trading_pairs;"
```

4. Проверьте подключение к Redis:
```bash
docker exec ownedcore_redis redis-cli ping
```

## 📝 Быстрый чек-лист

- [ ] Docker сервисы запущены (`docker ps`)
- [ ] API Gateway запущен (`curl http://localhost:8000/health`)
- [ ] Celery Worker запущен (`ps aux | grep celery`)
- [ ] Торговые пары добавлены (`python3 scripts/init_data.py`)
- [ ] Анализ запущен (`curl -X POST http://localhost:8000/api/force-analysis`)
- [ ] Данные появляются (`curl http://localhost:8000/api/pairs-status`)

