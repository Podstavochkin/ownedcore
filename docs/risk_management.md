# 🛡️ Управление рисками в live-торговле

## Обзор

Система автоматически отслеживает риски и останавливает торговлю при достижении критических лимитов для защиты капитала.

---

## 📊 Лимиты риска

### 1. Дневной лимит убытков
- **Лимит:** -5.0% от начального капитала за день
- **Проверка:** После каждой закрытой сделки
- **Действие:** Автоматическая остановка торговли

### 2. Серия убыточных сделок
- **Лимит:** 5 убыточных сделок подряд
- **Проверка:** После каждой закрытой сделки
- **Действие:** Автоматическая остановка торговли

---

## 🔄 Что происходит при достижении лимитов?

### При достижении дневного лимита убытков (-5%):

1. **Автоматическая остановка торговли:**
   - `live_trading_enabled` устанавливается в `false`
   - Все новые сигналы блокируются
   - Существующие открытые позиции остаются активными (не закрываются автоматически)

2. **Логирование:**
   ```
   🚫 Достигнут дневной лимит убытков: -5.23% (лимит: -5.0%). 
   Прибыльных: 2, убыточных: 8
   🛑 LIVE-ТОРГОВЛЯ АВТОМАТИЧЕСКИ ОСТАНОВЛЕНА
   ```

3. **Статус новых сигналов:**
   - При попытке разместить ордер для нового сигнала:
   - Статус: `risk_limit_exceeded`
   - Причина: "Достигнут дневной лимит убытков: -5.23%..."

### При 5 убыточных сделках подряд:

1. **Автоматическая остановка торговли:**
   - `live_trading_enabled` устанавливается в `false`
   - Все новые сигналы блокируются
   - Существующие открытые позиции остаются активными

2. **Логирование:**
   ```
   🚫 Серия из 5 убыточных сделок подряд (лимит: 5). 
   Торговля автоматически остановлена для защиты капитала.
   🛑 LIVE-ТОРГОВЛЯ АВТОМАТИЧЕСКИ ОСТАНОВЛЕНА
   ```

3. **Статус новых сигналов:**
   - При попытке разместить ордер для нового сигнала:
   - Статус: `risk_limit_exceeded`
   - Причина: "Серия из 5 убыточных сделок подряд..."

---

## 📋 Детальное описание работы

### Расчет дневного P&L

Система рассчитывает дневной P&L на основе:
- Всех закрытых сделок за сегодня (с 00:00 МСК)
- Только исполненных ордеров (`demo_order_id IS NOT NULL`)
- Учитывает комиссию (0.035% на вход и выход = 0.07% общая)

**Формула для LONG:**
```
P&L% = ((exit_price - entry_price) / entry_price) * 100 - 0.07%
```

**Формула для SHORT:**
```
P&L% = ((entry_price - exit_price) / entry_price) * 100 - 0.07%
```

### Отслеживание серии убытков

Система проверяет последние закрытые сделки (до 10 сделок) и считает убыточные подряд:
- Начинает с последней закрытой сделки
- Считает убыточные сделки подряд
- Прерывает счет при первой прибыльной или безубыточной сделке

**Пример:**
```
Последние сделки (от новой к старой):
1. -0.5% (убыток) → счет = 1
2. -0.3% (убыток) → счет = 2
3. -0.6% (убыток) → счет = 3
4. -0.4% (убыток) → счет = 4
5. -0.5% (убыток) → счет = 5 → ТОРГОВЛЯ ОСТАНОВЛЕНА
```

Если бы сделка #5 была прибыльной (+0.3%), счет сбросился бы до 0.

---

## 🔧 Где проверяются лимиты?

### 1. Перед размещением ордера (`demo_trade_executor.py`)
- Проверка выполняется в `place_order_for_signal()`
- Если лимит превышен → ордер не размещается
- Статус: `risk_limit_exceeded`

### 2. После закрытия позиции (`demo_trading_tasks.py`)
- Проверка выполняется в `watch_waiting_signals()` после коммита сессии
- Если лимит превышен → торговля останавливается
- Логируется критическое сообщение

---

## 🚨 Что НЕ происходит при остановке?

1. **Существующие открытые позиции НЕ закрываются автоматически**
   - Они остаются активными
   - Могут быть закрыты вручную через `/demo-trading` страницу
   - Могут быть закрыты по TP/SL

2. **Ожидающие сигналы НЕ отменяются**
   - Сигналы в статусе `WAITING_FOR_PRICE` остаются в БД
   - Но новые ордера для них не будут размещены

3. **Анализ рынка НЕ останавливается**
   - Система продолжает анализировать рынок
   - Сигналы продолжают генерироваться
   - Но ордера не размещаются

---

## 🔄 Как возобновить торговлю?

### После достижения дневного лимита:
1. Дождаться следующего дня (после 00:00 МСК)
2. Вручную включить торговлю через кнопку на странице `/signals` или `/demo-trading`
3. Или через API: `POST /api/trading/live/status` с `{"enabled": true}`

### После серии из 5 убытков:
1. Вручную включить торговлю через кнопку на странице `/signals` или `/demo-trading`
2. Или через API: `POST /api/trading/live/status` с `{"enabled": true}`
3. **Рекомендация:** Проанализировать причины убытков перед возобновлением

---

## 📊 Мониторинг рисков

### В логах:
- Все проверки лимитов логируются с уровнем `WARNING` или `CRITICAL`
- При остановке торговли логируется критическое сообщение

### В базе данных:
- Статусы сигналов с `risk_limit_exceeded` можно найти через:
  ```sql
  SELECT * FROM signals 
  WHERE demo_status = 'risk_limit_exceeded';
  ```

### Через API:
- Проверить статус торговли: `GET /api/trading/live/status`
- Проверить дневной P&L: можно рассчитать через закрытые сигналы за сегодня

---

## ⚙️ Настройка лимитов

Лимиты настраиваются в `core/trading/risk_manager.py`:

```python
DAILY_LOSS_LIMIT_PCT = -5.0  # Максимальный дневной убыток -5%
MAX_CONSECUTIVE_LOSSES = 5    # Максимальное количество убыточных сделок подряд
```

**Рекомендации:**
- Для консервативной торговли: `DAILY_LOSS_LIMIT_PCT = -3.0`, `MAX_CONSECUTIVE_LOSSES = 3`
- Для агрессивной торговли: `DAILY_LOSS_LIMIT_PCT = -7.0`, `MAX_CONSECUTIVE_LOSSES = 7`

---

## 🎯 Примеры сценариев

### Сценарий 1: Достижение дневного лимита
```
09:00 - Открыта сделка #1: -0.5%
10:00 - Открыта сделка #2: -0.8%
11:00 - Открыта сделка #3: -1.2%
12:00 - Открыта сделка #4: -1.5%
13:00 - Открыта сделка #5: -1.0%
→ Дневной убыток: -5.0% → ТОРГОВЛЯ ОСТАНОВЛЕНА
```

### Сценарий 2: Серия из 5 убытков
```
09:00 - Сделка #1: -0.5% (убыток #1)
10:00 - Сделка #2: -0.3% (убыток #2)
11:00 - Сделка #3: -0.6% (убыток #3)
12:00 - Сделка #4: -0.4% (убыток #4)
13:00 - Сделка #5: -0.5% (убыток #5) → ТОРГОВЛЯ ОСТАНОВЛЕНА
```

### Сценарий 3: Серия прервана прибыльной сделкой
```
09:00 - Сделка #1: -0.5% (убыток #1)
10:00 - Сделка #2: -0.3% (убыток #2)
11:00 - Сделка #3: -0.6% (убыток #3)
12:00 - Сделка #4: +1.2% (прибыль) → счет сброшен до 0
13:00 - Сделка #5: -0.5% (убыток #1) → торговля продолжается
```

---

## ✅ Преимущества системы управления рисками

1. **Автоматическая защита капитала** - не нужно вручную отслеживать убытки
2. **Предотвращение катастрофических дней** - как 01.12.2025 (-8.65%)
3. **Гибкость** - можно вручную возобновить торговлю после анализа
4. **Прозрачность** - все действия логируются и видны в системе

---

## 📝 Примечания

- Лимиты рассчитываются только на основе **исполненных** ордеров (`demo_order_id IS NOT NULL`)
- Комиссия учитывается автоматически (0.07% общая)
- Время считается по московскому времени (UTC+3)
- Дневной лимит сбрасывается автоматически в 00:00 МСК

