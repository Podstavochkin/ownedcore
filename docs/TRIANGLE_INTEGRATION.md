# Интеграция треугольников в систему уровней

**Дата:** 29.12.2025

---

## 🎯 Цель

Улучшить систему поиска и оценки уровней поддержки/сопротивления, учитывая активные треугольники на графике. Это позволит:
- Находить более сильные уровни (границы треугольника)
- Приоритизировать уровни внутри треугольника
- Фильтровать сигналы на основе направления треугольника
- Показывать границы треугольника и точку пробоя

---

## ✅ Реализовано

### 1. Получение активных треугольников

**Функция:** `get_active_triangle_for_pair(pair, timeframe, current_candle_index)`

- Получает активный (не пробитый) треугольник для пары и таймфрейма
- Вычисляет текущие границы треугольника на основе линий тренда
- Возвращает данные треугольника с текущими значениями support/resistance

**Пример:**
```python
triangle = analysis_engine.get_active_triangle_for_pair('ATOM/USDT', '1h', current_candle_index)
if triangle:
    support = triangle.get('current_support')
    resistance = triangle.get('current_resistance')
    pattern_type = triangle.get('pattern_type')  # ascending_triangle, descending_triangle, etc.
```

### 2. Улучшенная оценка силы уровней

**Функция:** `calculate_triangle_level_bonus(level, triangle, current_price)`

Добавляет бонус к score уровня на основе треугольника:

- **+30 баллов:** Уровень совпадает с границей треугольника (support/resistance)
- **+15 баллов:** Уровень внутри треугольника (чем ближе к границе, тем больше)
- **+10 баллов:** Уровень соответствует направлению треугольника
  - Восходящий треугольник → бонус для support (лонги)
  - Нисходящий треугольник → бонус для resistance (шорты)
- **-5 баллов:** Уровень вне треугольника (небольшой штраф)

**Максимальный бонус:** 50 баллов

### 3. Интеграция в поиск уровней

**Функция:** `find_potential_levels()`

Теперь:
1. Получает активный треугольник для пары и таймфрейма
2. Для каждого найденного уровня вычисляет бонус от треугольника
3. Добавляет бонус к итоговому score
4. Сохраняет информацию о треугольнике в метаданных уровня

**Результат:**
- Уровни границ треугольника получают максимальный приоритет
- Уровни внутри треугольника получают повышенный приоритет
- Уровни вне треугольника получают пониженный приоритет

### 4. Фильтрация сигналов на основе треугольника

**Функция:** `should_block_signal_by_filters()`

Теперь учитывает направление треугольника:

- **Восходящий треугольник (bullish):**
  - ✅ Разрешает LONG сигналы
  - ❌ Блокирует SHORT сигналы (кроме очень сильных уровней, score > 50)

- **Нисходящий треугольник (bearish):**
  - ✅ Разрешает SHORT сигналы
  - ❌ Блокирует LONG сигналы (кроме очень сильных уровней, score > 50)

- **Симметричный треугольник:**
  - ✅ Разрешает оба типа сигналов (нейтральный)

**Логика:**
- Если треугольник пробит (`is_confirmed=True`), фильтр не применяется
- Только активные (не пробитые) треугольники влияют на фильтрацию

---

## 📊 Пример работы

### Сценарий: ATOM/USDT на 1h с восходящим треугольником

**Треугольник:**
- Тип: `ascending_triangle`
- Направление: `bullish`
- Поддержка: 1.9920 (восходящая линия)
- Сопротивление: 2.0890 (горизонтальная линия)

**Найденные уровни:**
1. **Уровень @ 2.0890** (resistance) - граница треугольника
   - Базовый score: 35
   - Бонус треугольника: +30 (граница)
   - **Итоговый score: 65** ⭐ (максимальный приоритет)

2. **Уровень @ 1.9920** (support) - граница треугольника
   - Базовый score: 32
   - Бонус треугольника: +30 (граница) + 10 (направление)
   - **Итоговый score: 72** ⭐⭐ (максимальный приоритет)

3. **Уровень @ 2.0500** (support) - внутри треугольника
   - Базовый score: 28
   - Бонус треугольника: +12 (внутри, близко к границе)
   - **Итоговый score: 40**

4. **Уровень @ 1.9500** (support) - вне треугольника
   - Базовый score: 30
   - Бонус треугольника: -5 (вне треугольника)
   - **Итоговый score: 25**

**Результат:**
- Уровни границ треугольника (1.9920, 2.0890) получают максимальный приоритет
- Система будет генерировать LONG сигналы на уровне 1.9920
- SHORT сигналы на уровне 2.0890 будут блокироваться (восходящий треугольник)

---

## 🔧 Технические детали

### Структура данных треугольника

```python
{
    'pattern_type': 'ascending_triangle',  # или 'descending_triangle', 'symmetrical_triangle'
    'direction': 'bullish',  # или 'bearish', 'neutral'
    'support_level': 1.9920,  # Статическая граница поддержки
    'resistance_level': 2.0890,  # Статическая граница сопротивления
    'current_support': 1.9950,  # Текущая граница поддержки (вычисляется)
    'current_resistance': 2.0890,  # Текущая граница сопротивления (вычисляется)
    'breakout_point': 2.0420,  # Точка пробоя (середина)
    'is_active': True,
    'is_confirmed': False,
    'pattern_data': {
        'resistance_slope': -0.0001,
        'resistance_intercept': 2.0890,
        'support_slope': 0.0002,
        'support_intercept': 1.9920,
        'start_index': 100,
        'end_index': 200
    }
}
```

### Метаданные уровня

Каждый уровень теперь содержит:
```python
{
    'score': 65.0,  # Итоговый score
    'base_score': 35.0,  # Базовый score без треугольника
    'triangle_bonus': 30.0,  # Бонус от треугольника
    'triangle_pattern': 'ascending_triangle',  # Тип треугольника
    'triangle_direction': 'bullish'  # Направление треугольника
}
```

---

## 📈 Ожидаемые результаты

1. **Улучшение качества уровней:**
   - Система будет находить более сильные уровни (границы треугольника)
   - Меньше ложных уровней вне треугольника

2. **Улучшение фильтрации сигналов:**
   - В восходящем треугольнике приоритет лонгам
   - В нисходящем треугольнике приоритет шортам
   - Меньше сигналов против направления треугольника

3. **Повышение winrate:**
   - Торговля по границам треугольника более надежна
   - Учет направления треугольника снижает количество неудачных сделок

---

## 🔄 Следующие шаги

1. ✅ Получение активных треугольников
2. ✅ Улучшенная оценка силы уровней
3. ✅ Фильтрация сигналов на основе треугольника
4. ⏳ Отображение границ треугольника на графике (в API)
5. ⏳ Показ точки пробоя треугольника

---

**Статус:** ✅ Основная функциональность реализована

