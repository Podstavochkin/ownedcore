# 🛠️ Варианты настройки инфраструктуры

## Вариант 1: Docker (Рекомендуется) 🐳

### Преимущества:
- ✅ Изолированная среда
- ✅ Легко запустить/остановить
- ✅ Не засоряет систему
- ✅ Одинаковая среда на всех машинах

### Установка:
1. Установите Docker Desktop: https://www.docker.com/products/docker-desktop
2. Запустите Docker Desktop
3. Выполните:
```bash
docker-compose -f docker/docker-compose.yml up -d
```

### Проверка:
```bash
docker-compose -f docker/docker-compose.yml ps
```

---

## Вариант 2: Локальная установка (macOS) 🍎

### Преимущества:
- ✅ Работает без Docker
- ✅ Прямой доступ к сервисам
- ✅ Меньше нагрузки на систему

### Установка через Homebrew:

```bash
# Установите Homebrew если еще не установлен
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Запустите скрипт установки
./scripts/setup_local_services.sh
```

### Или вручную:

```bash
# PostgreSQL
brew install postgresql@15
brew services start postgresql@15

# Redis
brew install redis
brew services start redis

# RabbitMQ (опционально, для Celery)
brew install rabbitmq
brew services start rabbitmq
```

### Настройка PostgreSQL:

```bash
# Создайте базу данных
createdb ownedcore

# Или через psql
psql postgres -c "CREATE DATABASE ownedcore;"
```

### Проверка:

```bash
# PostgreSQL
psql -U postgres -d ownedcore -c "SELECT version();"

# Redis
redis-cli ping
# Должен вернуть: PONG

# RabbitMQ
rabbitmq-diagnostics ping
```

---

## Вариант 3: Облачные сервисы ☁️

### Для продакшена можно использовать:
- **PostgreSQL**: AWS RDS, Google Cloud SQL, DigitalOcean Managed Database
- **Redis**: AWS ElastiCache, Redis Cloud, Upstash
- **RabbitMQ**: CloudAMQP, AWS MQ

---

## Рекомендация для разработки:

**Используйте Docker** - это самый простой и надежный способ для локальной разработки.

---

## После установки сервисов:

Независимо от выбранного варианта, следующие шаги одинаковы:

1. ✅ Создайте .env файл
2. ✅ Настройте переменные окружения
3. ✅ Создайте миграции
4. ✅ Примените миграции
5. ✅ Мигрируйте данные

См. `docs/MVP_SETUP_GUIDE.md` для деталей.

