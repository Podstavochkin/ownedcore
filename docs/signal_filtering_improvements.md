# 🎯 Улучшение фильтрации сигналов

## Текущая ситуация

Сейчас система генерирует сигналы на основе:
- Касания уровня (первое или второе)
- Расстояния до уровня (максимум 1%)
- Фильтрации по тренду BTC (недавно добавлено)

**Проблема:** Нет проверки качества уровня (`level_score`), волатильности и объема перед генерацией сигнала.

---

## Предлагаемые улучшения

### 1. Минимальный порог `level_score` ✅

**Проблема:** Сейчас сигналы генерируются для всех уровней, независимо от их качества.

**Решение:** Добавить проверку минимального `level_score` перед генерацией сигнала.

**Текущий расчет `level_score`:**
```python
total_score = round(
    distance_score * 0.25 +      # Расстояние до уровня
    volume_score * 0.15 +         # Объем на уровне
    touch_score * 0.2 +           # Количество касаний
    freshness_score * 0.15 +      # Свежесть уровня
    approach_score * 0.15 +      # Качество подхода
    trend_bonus * 0.1,            # Бонус за тренд
    2
)
```

**Реальные данные из базы:**
- Максимальный `level_score`: 67.54
- Средний: 40.85
- Медиана: 41.06
- Только 6% сигналов имеют score 60-70
- 0% сигналов имеют score выше 70

**Корреляция с результатами:**
- 0-30: Win Rate 11.1%, средний результат -0.88%
- 30-40: Win Rate 23.8%, средний результат -0.22%
- 40-50: Win Rate 42.9%, средний результат +0.32% ✅
- 50-60: Win Rate 60.0%, средний результат +0.76% ✅✅
- 60+: только 1 сделка (недостаточно данных)

**Предлагаемый порог (на основе реальных данных):**
- **Минимум (реалистичный):** 40 баллов (Win Rate 42.9%)
- **Рекомендуется:** 50+ баллов (Win Rate 60.0%, но сигналов будет мало - ~12%)
- **Компромисс:** 45 баллов (баланс между качеством и количеством)

**Реализация:**
```python
# В analysis_engine.py перед генерацией сигнала (после строки 1304)
MIN_LEVEL_SCORE = 70  # Минимальный score для генерации сигнала

if should_generate_signal:
    level_score = level.get('score', 0)
    if level_score < MIN_LEVEL_SCORE:
        print(f"[{pair}] 🚫 БЛОКИРОВКА сигнала: level_score={level_score:.1f} < {MIN_LEVEL_SCORE} (слишком слабый уровень)")
        continue
```

**Ожидаемый эффект (на основе реальных данных):**
- При пороге 40: фильтруем ~50% сигналов (0-40 score), Win Rate улучшается с ~30% до ~43%
- При пороге 45: фильтруем ~60% сигналов (0-45 score), Win Rate улучшается до ~50%
- При пороге 50: фильтруем ~88% сигналов (0-50 score), Win Rate улучшается до ~60%, но сигналов будет очень мало

---

### 2. Проверка волатильности ✅

**Проблема:** В низкой волатильности уровни работают хуже, движения цены недостаточны для прибыли.

**Решение:** Добавить проверку минимальной волатильности перед генерацией сигнала.

**Метод расчета:** Используем `bybit_demo_client.get_symbol_volatility_pct()` (ATR-подобный подход).

**Предлагаемый порог:**
- **Минимум:** 0.3% волатильности (для 1m таймфрейма)
- **Рекомендуется:** 0.5%+ для более надежных сигналов

**Реализация:**
```python
# В analysis_engine.py перед генерацией сигнала
MIN_VOLATILITY_PCT = 0.3  # Минимальная волатильность 0.3%

if should_generate_signal:
    # Проверяем волатильность
    volatility_pct = bybit_demo_client.get_symbol_volatility_pct(
        mapped_symbol, 
        timeframe="1m", 
        lookback=30
    )
    
    if not volatility_pct or volatility_pct < MIN_VOLATILITY_PCT:
        print(f"[{pair}] 🚫 БЛОКИРОВКА сигнала: волатильность={volatility_pct:.3f}% < {MIN_VOLATILITY_PCT}% (слишком низкая)")
        continue
```

**Ожидаемый эффект:**
- Исключение сигналов в "мертвых" рынках
- Улучшение Win Rate на 3-5%
- Снижение количества сделок без движения

---

### 3. Проверка объема ✅

**Проблема:** В низком объеме уровни работают ненадежно, возможны ложные пробои.

**Решение:** Добавить проверку текущего объема относительно среднего.

**Предлагаемый порог:**
- **Минимум:** 1.2x среднего объема за последние 24 часа
- **Рекомендуется:** 1.5x+ для более надежных сигналов

**Реализация:**
```python
# В analysis_engine.py перед генерацией сигнала
MIN_VOLUME_RATIO = 1.2  # Минимальный объем относительно среднего

if should_generate_signal:
    # Рассчитываем средний объем за последние 24 часа
    recent_candles = candles_15m[-96:]  # Последние 96 свечей (24 часа для 15m)
    if len(recent_candles) >= 24:
        avg_volume = sum(c['volume'] for c in recent_candles) / len(recent_candles)
        current_volume = candles_15m[-1]['volume']
        volume_ratio = current_volume / avg_volume if avg_volume > 0 else 0
        
        if volume_ratio < MIN_VOLUME_RATIO:
            print(f"[{pair}] 🚫 БЛОКИРОВКА сигнала: volume_ratio={volume_ratio:.2f} < {MIN_VOLUME_RATIO} (слишком низкий объем)")
            continue
```

**Ожидаемый эффект:**
- Исключение сигналов в низколиквидных условиях
- Улучшение Win Rate на 3-5%
- Снижение количества ложных пробоев

---

## Комбинированный эффект

При применении всех трех фильтров:

**Ожидаемые результаты:**
- **Снижение количества сигналов:** на 40-50%
- **Улучшение Win Rate:** с 50-60% до 60-70%
- **Снижение среднего убытка:** на 20-30%
- **Улучшение среднего профита:** на 10-15%

**Компромисс:**
- Меньше сигналов, но более качественных
- Меньше сделок, но выше процент прибыльных

---

## Пороговые значения (рекомендации)

### Консервативный подход (меньше сигналов, выше качество):
```python
MIN_LEVEL_SCORE = 50      # Win Rate 60.0%, но сигналов будет ~12%
MIN_VOLATILITY_PCT = 0.5
MIN_VOLUME_RATIO = 1.5
```
**Ожидаемый эффект:** Очень мало сигналов, но высокий Win Rate (~60%)

### Умеренный подход (баланс) - РЕКОМЕНДУЕТСЯ:
```python
MIN_LEVEL_SCORE = 45      # Win Rate ~50%, сигналов будет ~30-40%
MIN_VOLATILITY_PCT = 0.3
MIN_VOLUME_RATIO = 1.2
```
**Ожидаемый эффект:** Баланс между качеством и количеством сигналов

### Агрессивный подход (больше сигналов):
```python
MIN_LEVEL_SCORE = 40      # Win Rate 42.9%, сигналов будет ~50%
MIN_VOLATILITY_PCT = 0.2
MIN_VOLUME_RATIO = 1.0
```
**Ожидаемый эффект:** Больше сигналов, но Win Rate ниже (~43%)

### Минимальный подход (фильтруем только очень слабые):
```python
MIN_LEVEL_SCORE = 30      # Фильтруем только самые слабые (Win Rate 11.1%)
MIN_VOLATILITY_PCT = 0.2
MIN_VOLUME_RATIO = 1.0
```
**Ожидаемый эффект:** Убираем только самые плохие сигналы (0-30 score)

**Рекомендация:** Начать с **умеренного подхода (45)** и корректировать на основе результатов. Если сигналов будет слишком мало, снизить до 40. Если качество недостаточное, повысить до 50.

---

## Порядок проверок

Рекомендуемый порядок проверок перед генерацией сигнала:

1. ✅ Расстояние до уровня (уже есть)
2. ✅ Фильтрация по тренду BTC (уже есть)
3. 🆕 Минимальный `level_score`
4. 🆕 Минимальная волатильность
5. 🆕 Минимальный объем

**Почему такой порядок:**
- Быстрые проверки (расстояние, тренд) выполняются первыми
- Более затратные проверки (волатильность, объем) выполняются только для перспективных сигналов

---

## Пример логирования

После добавления фильтров логи будут выглядеть так:

```
[ETH/USDT] Первое живое касание уровня! Генерируем сигнал (расстояние: 0.15%)...
[ETH/USDT] ✅ Сигнал LONG разрешен: общий тренд рынка UP
[ETH/USDT] ✅ level_score=72.5 >= 70 (прошел)
[ETH/USDT] ✅ волатильность=0.45% >= 0.3% (прошел)
[ETH/USDT] ✅ volume_ratio=1.35 >= 1.2 (прошел)
[ETH/USDT] ГЕНЕРИРУЕМ СИГНАЛ LONG на уровне 2950.00
```

Или при блокировке:

```
[ETH/USDT] Первое живое касание уровня! Генерируем сигнал (расстояние: 0.15%)...
[ETH/USDT] ✅ Сигнал LONG разрешен: общий тренд рынка UP
[ETH/USDT] 🚫 БЛОКИРОВКА сигнала: level_score=55.2 < 70 (слишком слабый уровень)
```

---

## Метрики для мониторинга

После внедрения нужно отслеживать:

1. **Процент заблокированных сигналов:**
   - По причине низкого `level_score`
   - По причине низкой волатильности
   - По причине низкого объема

2. **Win Rate до и после:**
   - Сравнить Win Rate сигналов, прошедших фильтры
   - С Win Rate всех сигналов (если бы фильтров не было)

3. **Количество сигналов:**
   - Снижение количества сигналов
   - Компенсируется ли качеством

---

## Следующие шаги

1. ✅ Реализовать проверку минимального `level_score`
2. ✅ Реализовать проверку волатильности
3. ✅ Реализовать проверку объема
4. 📊 Собрать статистику за 1-2 недели
5. 📊 Скорректировать пороговые значения на основе результатов

