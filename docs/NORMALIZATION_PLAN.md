# 📋 План нормализации торговой системы

На основе комплексного анализа 229 сделок с 10.12.2024

## 📊 Текущее состояние

- **Winrate:** 27.51% (63 прибыльных из 229)
- **Profit Factor:** 0.97 (< 1.0 - система убыточна)
- **Общий P&L:** -3.90%
- **Средний убыток:** -0.74%
- **Средняя прибыль:** +1.90%

## 🎯 Целевые показатели

- **Winrate:** ≥ 40% (цель: 45-50%)
- **Profit Factor:** ≥ 1.2 (цель: 1.5+)
- **Общий P&L:** положительный

---

## 🔧 Этап 1: Фильтрация по Level Score

### Проблема
Сейчас генерируются сигналы для всех уровней, независимо от качества. Анализ показывает:
- **Level Score 0-30:** Winrate 25.71%, P&L -0.10%
- **Level Score 30-40:** Winrate 33.33%, P&L +0.08% ✅
- **Level Score 40-50:** Winrate 26.15%, P&L -0.01%
- **Level Score 50-60:** Winrate 24.00%, P&L -0.05%
- **Level Score 60-70:** Winrate 20.00%, P&L -0.28% ❌

### Решение
**Установить минимальный порог level_score = 30**

**Ожидаемый эффект:**
- Фильтруем ~15% худших сигналов (0-30 score)
- Winrate улучшается с 27.5% до ~32%
- Убираем самые слабые уровни

**Реализация:**
```python
# В core/analysis_engine.py и tasks/level_touch_tasks.py
MIN_LEVEL_SCORE = 30  # Минимальный порог на основе анализа

if should_generate_signal:
    level_score = level.get('score', 0)
    if level_score < MIN_LEVEL_SCORE:
        logger.info(f"[{pair}] 🚫 БЛОКИРОВКА: level_score={level_score:.1f} < {MIN_LEVEL_SCORE}")
        continue
```

**Приоритет:** 🔴 Высокий

---

## 🔧 Этап 2: Исключение проблемных трендов

### Проблема
Анализ показывает критические проблемы с определенными трендами:
- **DOWN_SIDEWAYS:** 0% winrate (7 сделок), P&L -0.74% ❌
- **UP_SIDEWAYS:** 16.67% winrate (18 сделок), P&L -0.20% ❌

### Решение
**Полностью исключить сигналы при боковых трендах (SIDEWAYS)**

**Ожидаемый эффект:**
- Фильтруем ~11% проблемных сигналов (25 сделок)
- Улучшаем общий winrate на ~2-3%

**Реализация:**
```python
# В core/analysis_engine.py
# Перед генерацией сигнала проверяем тренд

if trend_1h and ('SIDEWAYS' in trend_1h or trend_1h.startswith('SIDEWAYS')):
    logger.info(f"[{pair}] 🚫 БЛОКИРОВКА: боковой тренд ({trend_1h}) - 0% winrate")
    continue
```

**Приоритет:** 🔴 Высокий

---

## 🔧 Этап 3: Приоритизация лучших комбинаций

### Находки из анализа

**Лучшие комбинации (Winrate ≥ 50%):**
1. **1h + UP_STRONG:** 83.3% winrate, +1.30% P&L ✅✅✅
2. **DOWN_WEAK + Level Score 30-40:** 60% winrate, +0.69% P&L ✅✅
3. **1h + DOWN_STRONG:** 62.5% winrate, +0.65% P&L ✅✅
4. **UP_STRONG + Level Score 0-30:** 75% winrate, +0.98% P&L ✅✅

**Худшие комбинации (Winrate < 25%):**
1. **15m + UP_STRONG:** 10% winrate, -0.64% P&L ❌
2. **UP_WEAK + Level Score 0-30:** 10.5% winrate, -0.49% P&L ❌
3. **DOWN_WEAK + Level Score 50-60:** 8.3% winrate, -0.39% P&L ❌

### Решение
**Добавить бонусы/приоритеты для лучших комбинаций**

**Реализация:**
```python
# В core/analysis_engine.py
# После проверки базовых условий, добавляем бонусы

def calculate_signal_priority(trend_1h, level_score, timeframe):
    """Рассчитывает приоритет сигнала на основе лучших комбинаций"""
    priority = 0
    
    # Бонусы за лучшие комбинации
    if timeframe == '1h' and trend_1h == 'UP_STRONG':
        priority += 10  # Максимальный приоритет
    elif timeframe == '1h' and trend_1h == 'DOWN_STRONG':
        priority += 8
    elif trend_1h == 'DOWN_WEAK' and 30 <= level_score < 40:
        priority += 7
    elif trend_1h == 'UP_STRONG' and level_score < 30:
        priority += 6
    
    # Штрафы за худшие комбинации
    if timeframe == '15m' and trend_1h == 'UP_STRONG':
        priority -= 5  # Сильно снижаем приоритет
    elif trend_1h == 'UP_WEAK' and level_score < 30:
        priority -= 4
    
    return priority

# Использование
priority = calculate_signal_priority(trend_1h, level_score, timeframe)
if priority < -3:  # Критически низкий приоритет
    logger.info(f"[{pair}] 🚫 БЛОКИРОВКА: низкий приоритет ({priority})")
    continue
```

**Приоритет:** 🟡 Средний

---

## 🔧 Этап 4: Улучшение фильтрации по таймфреймам

### Проблема
- **15m:** 25.19% winrate (135 сделок) - самый низкий
- **1h:** 35.42% winrate (48 сделок) - лучший ✅
- **4h:** 26.09% winrate (46 сделок)

### Решение
**Снизить приоритет сигналов на 15m, повысить на 1h**

**Реализация:**
```python
# В core/analysis_engine.py
TIMEFRAME_PRIORITY = {
    '1h': 1.0,    # Высокий приоритет
    '4h': 0.8,    # Средний приоритет
    '15m': 0.6    # Низкий приоритет (но не блокируем полностью)
}

# При генерации сигнала
timeframe_priority = TIMEFRAME_PRIORITY.get(timeframe, 0.5)
if timeframe_priority < 0.5:
    # Для 15m требуем более высокий level_score
    if level_score < 35:  # Повышенный порог для 15m
        logger.info(f"[{pair}] 🚫 БЛОКИРОВКА 15m: level_score={level_score:.1f} < 35")
        continue
```

**Приоритет:** 🟡 Средний

---

## 🔧 Этап 5: Дополнительные фильтры

### 5.1 Фильтр по расстоянию до уровня

**Анализ показывает:**
- Сделки с distance_percent < 0.5% показывают лучшие результаты
- Сделки с distance_percent > 1.0% показывают худшие результаты

**Реализация:**
```python
# Уже есть проверка distance <= 0.7%, можно ужесточить до 0.5%
MAX_DISTANCE_FOR_SIGNAL = 0.5  # Снижаем с 0.7% до 0.5%
```

### 5.2 Фильтр по количеству тестов

**Анализ показывает:**
- test_count 3-5: хорошие результаты (36-50% winrate)
- test_count > 20: плохие результаты (0% winrate в большинстве случаев)

**Реализация:**
```python
# Исключаем уровни с слишком большим количеством тестов
MAX_TEST_COUNT = 20
if test_count > MAX_TEST_COUNT:
    logger.info(f"[{pair}] 🚫 БЛОКИРОВКА: слишком много тестов ({test_count})")
    continue
```

**Приоритет:** 🟢 Низкий

---

## 📅 План внедрения

### Неделя 1: Критические фильтры
1. ✅ Этап 1: Минимальный level_score = 30
2. ✅ Этап 2: Исключение SIDEWAYS трендов

**Ожидаемый результат:** Winrate улучшится до ~32-35%

### Неделя 2: Оптимизация
3. ✅ Этап 3: Приоритизация лучших комбинаций
4. ✅ Этап 4: Фильтрация по таймфреймам

**Ожидаемый результат:** Winrate улучшится до ~38-42%

### Неделя 3: Дополнительные улучшения
5. ✅ Этап 5: Дополнительные фильтры

**Ожидаемый результат:** Winrate достигнет 40-45%

---

## 📊 Метрики для мониторинга

После каждого этапа отслеживать:
1. **Winrate** (цель: ≥ 40%)
2. **Profit Factor** (цель: ≥ 1.2)
3. **Количество сигналов** (не должно упасть критически)
4. **Средний P&L** (цель: > 0%)

---

## ⚠️ Важные замечания

1. **Постепенное внедрение:** Не применять все фильтры сразу, тестировать поэтапно
2. **Мониторинг:** Отслеживать количество сигналов - не должно быть слишком мало
3. **Гибкость:** Параметры должны быть настраиваемыми через конфигурацию
4. **Откат:** Должна быть возможность быстро откатить изменения

---

## 🔄 Обратная связь

После внедрения каждого этапа:
1. Запустить анализ на новых данных (через 1-2 недели)
2. Сравнить результаты с предыдущим периодом
3. При необходимости скорректировать параметры
4. Перейти к следующему этапу

---

## 📝 Файлы для изменения

1. `core/analysis_engine.py` - основная логика генерации сигналов
2. `tasks/level_touch_tasks.py` - проверка касаний уровней
3. `core/config.py` - конфигурация фильтров (новые параметры)
4. `docs/signal_filtering_improvements.md` - обновить документацию

---

## ✅ Чеклист готовности

- [ ] Этап 1: Минимальный level_score
- [ ] Этап 2: Исключение SIDEWAYS
- [ ] Этап 3: Приоритизация комбинаций
- [ ] Этап 4: Фильтрация по таймфреймам
- [ ] Этап 5: Дополнительные фильтры
- [ ] Тестирование на исторических данных
- [ ] Мониторинг в production
- [ ] Документация обновлена

