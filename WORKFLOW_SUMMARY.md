# 📋 Краткое руководство по работе с проектом

## 🚀 Запуск проекта

### Вариант 1: Автоматический запуск (рекомендуется)

```bash
./START_ALL.sh
```

**Что делает:**
- ✅ Запускает Docker контейнеры (PostgreSQL, Redis, RabbitMQ)
- ✅ Запускает API Gateway
- ✅ Запускает Celery Worker
- ✅ Запускает Celery Beat (периодические задачи)
- ✅ Проверяет статус всех сервисов

### Вариант 2: Ручной запуск (для разработки)

Если нужно запускать компоненты в отдельных терминалах для мониторинга:

**Терминал 1: API Gateway**
```bash
./START_API.sh
```

**Терминал 2: Celery Worker**
```bash
./START_CELERY.sh
```

**Терминал 3: Celery Beat**
```bash
./START_BEAT.sh
```

---

## 🛑 Остановка проекта

### Автоматическая остановка

```bash
./STOP_ALL.sh
```

**Что делает:**
- ✅ Останавливает API Gateway
- ✅ Останавливает Celery Worker
- ✅ Останавливает Celery Beat
- ✅ Останавливает Docker контейнеры

### Ручная остановка

```bash
# Остановка отдельных компонентов
pkill -f "services/api_gateway/main.py"  # API Gateway
pkill -f "celery.*worker"                # Celery Worker
pkill -f "celery.*beat"                  # Celery Beat

# Остановка Docker контейнеров
cd docker && docker-compose down
```

---

## ⏰ Периодические задачи

Система автоматически выполняет:

| Задача | Частота | Описание |
|--------|---------|----------|
| `analyze_all_pairs` | Каждые 5 минут | Анализ всех пар, поиск уровней, проверка пробитых уровней |
| `update_signals_pnl` | Каждую минуту | Обновление P&L сигналов, расчет прибыли/убытка |
| `check_level_touches` | Каждую минуту | Проверка касаний уровней для скальпинга |

---

## 📁 Структура скриптов

```
.
├── START_ALL.sh       # Запуск всех компонентов
├── STOP_ALL.sh        # Остановка всех компонентов
├── START_API.sh       # Запуск API Gateway
├── START_CELERY.sh    # Запуск Celery Worker
└── START_BEAT.sh      # Запуск Celery Beat
```

---

## 🔍 Полезные команды

### Проверка статуса

```bash
# Health check
curl http://localhost:8000/health

# Статус задач
curl http://localhost:8000/api/tasks

# Статус пар
curl http://localhost:8000/api/pairs-status
```

### Принудительный анализ

```bash
# Анализ всех пар
curl -X POST http://localhost:8000/api/force-analysis

# Обновление P&L
curl -X POST http://localhost:8000/api/update-signals-pnl
```

### Просмотр логов

```bash
# API Gateway
tail -f /tmp/api_gateway.log

# Celery Worker
tail -f /tmp/celery_worker.log

# Celery Beat
tail -f /tmp/celery_beat.log

# Все логи одновременно
tail -f /tmp/api_gateway.log /tmp/celery_worker.log /tmp/celery_beat.log
```

---

## 🎯 Типичные сценарии

### Первый запуск

```bash
# 1. Запустить все
./START_ALL.sh

# 2. Подождать 1-2 минуты

# 3. Проверить
curl http://localhost:8000/health

# 4. Открыть Dashboard
open http://localhost:8000/dashboard
```

### Перезапуск после изменений

```bash
# 1. Остановить
./STOP_ALL.sh

# 2. Запустить заново
./START_ALL.sh
```

### Перезапуск только Celery

```bash
# Остановить
pkill -f "celery.*worker"
pkill -f "celery.*beat"

# Запустить в отдельных терминалах
./START_CELERY.sh  # Терминал 1
./START_BEAT.sh    # Терминал 2
```

---

## 📖 Подробная документация

См. [PROJECT_WORKFLOW.md](./PROJECT_WORKFLOW.md) для полного руководства.

---

**Готово!** 🎉

