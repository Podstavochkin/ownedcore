# 📋 Руководство по работе с проектом OwnedCore

## 🚀 Быстрый старт

### Вариант 1: Автоматический запуск всех компонентов (рекомендуется)

```bash
./START_ALL.sh
```

Этот скрипт:
- ✅ Запускает Docker контейнеры (PostgreSQL, Redis, RabbitMQ)
- ✅ Запускает API Gateway
- ✅ Запускает Celery Worker
- ✅ Запускает Celery Beat (периодические задачи)
- ✅ Проверяет статус всех сервисов

### Вариант 2: Ручной запуск компонентов

Если нужно запускать компоненты отдельно:

#### 1. Запуск Docker контейнеров
```bash
cd docker
docker-compose up -d
```

#### 2. Запуск API Gateway
```bash
./START_API.sh
```

#### 3. Запуск Celery Worker
```bash
./START_CELERY.sh
```

#### 4. Запуск Celery Beat (периодические задачи)
```bash
./START_BEAT.sh
```

---

## 🛑 Остановка проекта

### Автоматическая остановка всех компонентов

```bash
./STOP_ALL.sh
```

Этот скрипт:
- ✅ Останавливает API Gateway
- ✅ Останавливает Celery Worker
- ✅ Останавливает Celery Beat
- ✅ Останавливает Docker контейнеры

### Ручная остановка компонентов

```bash
# Остановка API Gateway
pkill -f "services/api_gateway/main.py"

# Остановка Celery Worker
pkill -f "celery.*worker"

# Остановка Celery Beat
pkill -f "celery.*beat"

# Остановка Docker контейнеров
cd docker
docker-compose down
```

---

## 📊 Компоненты системы

### 1. Docker сервисы
- **PostgreSQL** (порт 5432) - база данных
- **Redis** (порт 6379) - кэширование
- **RabbitMQ** (порт 5672, UI: 15672) - очереди сообщений

### 2. API Gateway (порт 8000)
- Главная точка входа для всех API запросов
- Отдает фронтенд (Dashboard, Signals)
- REST API для данных

### 3. Celery Worker
- Обрабатывает задачи из очередей `analysis` и `signals`
- Выполняет анализ торговых пар
- Обновляет P&L сигналов
- Проверяет касания уровней

### 4. Celery Beat
- Планировщик периодических задач
- Запускает задачи по расписанию

---

## ⏰ Расписание задач

### Каждые 5 минут:
- **Анализ всех торговых пар** (`analyze_all_pairs`)
  - Поиск уровней поддержки/сопротивления
  - Определение трендов
  - Проверка пробитых уровней
  - Добавление новых уровней

### Каждую минуту:
- **Обновление P&L сигналов** (`update_signals_pnl`)
  - Расчет прибыли/убытка
  - Определение достижения порогов (1.5% / -0.5%)
  - Обновление максимальных движений

- **Проверка касаний уровней** (`check_level_touches`) - **НОВОЕ!**
  - Быстрая проверка всех активных уровней
  - Генерация сигналов при касании
  - Оперативность для скальпинга

---

## 🔄 Типичные сценарии работы

### Сценарий 1: Первый запуск проекта

```bash
# 1. Запустить все компоненты
./START_ALL.sh

# 2. Подождать 1-2 минуты для инициализации

# 3. Проверить статус
curl http://localhost:8000/health

# 4. Открыть Dashboard
open http://localhost:8000/dashboard
```

### Сценарий 2: Перезапуск после изменений в коде

```bash
# 1. Остановить все
./STOP_ALL.sh

# 2. Запустить заново
./START_ALL.sh
```

### Сценарий 3: Перезапуск только Celery (после изменений в задачах)

```bash
# 1. Остановить Celery Worker и Beat
pkill -f "celery.*worker"
pkill -f "celery.*beat"

# 2. Запустить заново
./START_CELERY.sh  # в одном терминале
./START_BEAT.sh    # в другом терминале
```

### Сценарий 4: Принудительный перерасчет всех пар

```bash
# Запустить анализ через API
curl -X POST http://localhost:8000/api/force-analysis

# Проверить статус задачи
curl http://localhost:8000/api/tasks
```

### Сценарий 5: Проверка логов

```bash
# Логи API Gateway
tail -f /tmp/api_gateway.log

# Логи Celery Worker
tail -f /tmp/celery_worker.log

# Логи Celery Beat
tail -f /tmp/celery_beat.log
```

---

## 📁 Структура скриптов

```
.
├── START_ALL.sh          # Запуск всех компонентов
├── STOP_ALL.sh           # Остановка всех компонентов
├── START_API.sh          # Запуск API Gateway
├── START_CELERY.sh       # Запуск Celery Worker
├── START_BEAT.sh         # Запуск Celery Beat (периодические задачи)
└── docker/
    └── docker-compose.yml # Docker контейнеры
```

---

## 🎯 Полезные команды

### Проверка статуса сервисов

```bash
# Проверка API Gateway
curl http://localhost:8000/health

# Проверка RabbitMQ
docker exec ownedcore_rabbitmq rabbitmq-diagnostics ping

# Проверка PostgreSQL
docker exec ownedcore_postgres pg_isready -U postgres

# Проверка Redis
docker exec ownedcore_redis redis-cli ping
```

### Просмотр активных задач Celery

```bash
curl http://localhost:8000/api/tasks
```

### Принудительный анализ

```bash
# Анализ всех пар
curl -X POST http://localhost:8000/api/force-analysis

# Обновление P&L сигналов
curl -X POST http://localhost:8000/api/update-signals-pnl
```

### Просмотр данных

```bash
# Статус всех пар
curl http://localhost:8000/api/pairs-status

# Все сигналы
curl http://localhost:8000/api/signals

# Все уровни
curl http://localhost:8000/api/levels
```

---

## 🔍 Мониторинг и отладка

### Просмотр логов в реальном времени

```bash
# Все логи одновременно
tail -f /tmp/api_gateway.log /tmp/celery_worker.log /tmp/celery_beat.log

# Только ошибки
grep -i error /tmp/*.log

# Логи конкретной пары
grep "\[BTC/USDT\]" /tmp/celery_worker.log
```

### Проверка процессов

```bash
# Процессы Python
ps aux | grep python

# Процессы Celery
ps aux | grep celery

# Docker контейнеры
docker ps
```

### Проверка базы данных

```bash
# Подключение к PostgreSQL
docker exec -it ownedcore_postgres psql -U postgres -d ownedcore

# Просмотр уровней
SELECT t.symbol, l.price, l.level_type, l.created_at 
FROM levels l 
JOIN trading_pairs t ON t.id = l.pair_id 
WHERE l.is_active = true 
ORDER BY t.symbol, l.price;

# Просмотр сигналов
SELECT s.id, t.symbol, s.signal_type, s.level_price, s.status, s.timestamp
FROM signals s
JOIN trading_pairs t ON t.id = s.pair_id
ORDER BY s.timestamp DESC
LIMIT 10;
```

---

## 🚨 Решение проблем

### Проблема: "celery: command not found"
**Решение**: Скрипты используют `python3 -m celery`, что должно работать. Если проблема сохраняется:
```bash
pip3 install celery
```

### Проблема: "ModuleNotFoundError: No module named 'tasks'"
**Решение**: Убедитесь, что запускаете скрипты из корня проекта:
```bash
cd "/Users/andrejpodstavockin/Documents/OwnedCore- CryptoProject v0.1 04"
./START_CELERY.sh
```

### Проблема: RabbitMQ не доступен
**Решение**: 
```bash
cd docker
docker-compose up -d rabbitmq
```

### Проблема: База данных не подключена
**Решение**:
```bash
# Проверить контейнер
docker ps | grep postgres

# Проверить логи
docker logs ownedcore_postgres

# Перезапустить
docker-compose -f docker/docker-compose.yml restart postgres
```

### Проблема: Уровни не обновляются
**Решение**:
```bash
# Принудительный анализ
curl -X POST http://localhost:8000/api/force-analysis

# Проверить логи Celery Worker
tail -f /tmp/celery_worker.log | grep "АНАЛИЗ ПАРЫ"
```

---

## 📝 Рекомендации по работе

1. **Всегда используйте `START_ALL.sh` для первого запуска** - это гарантирует правильную инициализацию всех компонентов

2. **Проверяйте логи после запуска** - убедитесь, что все сервисы запустились без ошибок

3. **Не останавливайте Docker контейнеры во время работы** - они должны работать постоянно

4. **Используйте отдельные терминалы для каждого компонента** при ручном запуске для удобного мониторинга

5. **Проверяйте статус через API** перед началом работы:
   ```bash
   curl http://localhost:8000/health
   ```

---

## 🎓 Дополнительная информация

- **Dashboard**: http://localhost:8000/dashboard
- **Signals**: http://localhost:8000/signals
- **API Docs**: http://localhost:8000/docs
- **RabbitMQ UI**: http://localhost:15672 (guest/guest)

---

**Готово!** Теперь вы знаете, как работать с проектом. 🚀

